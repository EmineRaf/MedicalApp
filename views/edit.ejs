<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier un utilisateur</title>
    
</head>
<body>
  <a href="/dashboard">
    <button class="acceuilButton">Retour à l'acceuil</button>
  </a>    
    <h2>Modifier l'utilisateur</h2>
    <form action="/update/<%= user.id_patient%>" method="POST">
        <fieldset>
            <label for="first-name">First Name: <input id="first-name" name="nom" type="text" value="<%= user.nom %>" /></label><br>
            <label for="last-name">Last Name: <input id="last-name" name="prenom" type="text" value="<%= user.prenom %>"  /></label><br>
            <label for="email">Email: <input id="email" name="email" type="email" value="<%= user.email %>" /></label><br>
            <label for="phone-number">phone number: <input id="phone-number" name="tel" type="tel"  value="<%= user.num_tel %>"/></label><br>
            <label for="date-of-birth">Date of Birth: <input id="date-of-birth" name="dateNaissance" type="date" value="<%= user.date_naissance %>" required><br>
            <label for="Taille">Taille en Cm <input id="taille" name="taille" type="number" step="any"value="<%= user.taille %>" /></label><br>
            <label for="Poids">Poids en Kg <input id="poids" name="poids" type="number" step="any"value="<%= user.poids %>" /></label><br>
                <input type="submit" value="Modifier">
        </fieldset>
    </form>
    <style>
      /* Réinitialisation et styles de base */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f0f8ff;
    color: #333;
    line-height: 1.6;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 20px;
    position: relative;
}

/* Arrière-plan flou avec overlay bleu */
body::before {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("doctors.png");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    filter: blur(8px);
    z-index: -2;
}

body::after {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 105, 255, 0.2);
    z-index: -1;
}

/* Titre principal */
h2 {
    color: #0066ff;
    text-align: center;
    margin-bottom: 2rem;
    font-size: 2rem;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
}

/* Formulaire de modification */
form {
    background-color: white;
    border-radius: 10px;
    padding: 2rem;
    box-shadow: 0 8px 20px rgba(0, 68, 204, 0.15);
    border: 1px solid #e0e0e0;
    width: 100%;
    max-width: 500px;
}

fieldset {
    border: none;
    padding: 0;
}

label {
    display: block;
    margin-bottom: 1.5rem;
    font-weight: 500;
    color: #555;
}

input[type="text"],
input[type="email"],
input[type="tel"],
input[type="date"],
input[type="number"] {
    width: 100%;
    padding: 0.8rem;
    margin-top: 0.5rem;
    border: 2px solid #ddd;
    border-radius: 6px;
    font-size: 1rem;
    transition: all 0.3s;
}

input[type="text"]:focus,
input[type="email"]:focus,
input[type="tel"]:focus,
input[type="date"]:focus,
input[type="number"]:focus {
    border-color: #0066ff;
    box-shadow: 0 0 0 3px rgba(0, 102, 255, 0.2);
    outline: none;
}

input[type="submit"] {
    background: linear-gradient(135deg, #0066ff, #0044cc);
    color: white;
    border: none;
    padding: 1rem 2rem;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 6px;
    cursor: pointer;
    width: 100%;
    transition: all 0.3s;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-top: 1rem;
}

input[type="submit"]:hover {
    background: linear-gradient(135deg, #0044cc, #0066ff);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(0, 68, 204, 0.3);
}

/* Responsive design */
@media (max-width: 600px) {
    body {
        padding: 15px;
    }
    
    h2 {
        font-size: 1.5rem;
    }
    
    form {
        padding: 1.5rem;
    }
}
.acceuilButton{
    display: inline-block;
    background-color: #2AA8FF;
    color: aliceblue;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    font-size: 1rem;
    cursor: pointer;
    margin-top: 20px;
    

}

.acceuilButton:hover{
    background-color: #358ece;
}

    </style>
</body>
</html>
